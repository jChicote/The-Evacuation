using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HangarData : MonoBehaviour
{
    // This class should be responsible in both handling methods and access the data related to the player's hangar.
    // This should hold no information related to the shop or with transactions.
    // Must be also responsible in reading and writing data to the game files.


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
